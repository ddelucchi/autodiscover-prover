/**
 * @file Normalizer.cpp
 * @brief Implementation of GOD canonicalizer and rewrite system
 * 
 * All GODNormalizer, Normalizer, and RewriteRule methods are implemented inline in Normalizer.hpp.
 * This file exists as a compilation unit that includes the header.
 */

#include "Normalizer.hpp"

// All methods are defined inline in the header.

